This directory will contain the Sphinx-specific document makefiles for both linix/unix systems and Windows.
Until source code exists in the `source` directory, there is no need to setup the documentation system.
